{
  "name": "lightflux",
  "version": "0.1.0",
  "description": "Manage tasks and milestones in the local LightFlux desktop application.",
  "license": "MIT",
  "type": "module",
  "bin": {
    "lightflux": "./src/cli.mjs"
  },
  "files": [
    "assets",
    "src",
    "README.md",
    "LICENSE"
  ],
  "scripts": {
    "check": "node --check src/api.mjs && node --check src/cli.mjs && node --check src/config.mjs && node --check src/content.mjs && node --check src/context.mjs && node --check src/credentials.mjs && node --check src/desktop.mjs && node --check src/local.mjs && node --check src/milestone.mjs && bash -n scripts/install.sh && npm test",
    "prepublishOnly": "npm run check",
    "test": "node --test tests/*.test.mjs"
  },
  "engines": {
    "node": ">=22"
  },
  "publishConfig": {
    "access": "public"
  }
}
